# In a past life, images.jeremyfelt.com was used for all uploaded content in
# WordPress as well as various image resources used manually. We now redirect
# any of these requests to the new content.jeremyfelt.com domain accordingly.
server {
    listen         80;
    server_name    images.jeremyfelt.com;
    rewrite ^      http://content.jeremyfelt.com/images$request_uri? permanent;
}

# Dig the non www and reroute.
server {
    listen         80;
    server_name    www.jeremyfelt.com;
    rewrite ^      http://jeremyfelt.com$request_uri? permanent;
}

# Handle all the main site requests.
server {
    listen         80;
    server_name    jeremyfelt.com dev.jeremyfelt.com;
    root           /srv/web/jeremyfelt.com/www/wordpress;

    access_log     /var/log/nginx/jeremyfelt-access.log;
    error_log      /var/log/nginx/jeremyfelt-error.log;

    location / {
        index index.php;
        try_files $uri $uri/ /index.php?$args;
    }

    rewrite ^/sitemap_index\.xml$ /index.php?sitemap=1 last;
    rewrite ^/([^/]+?)-sitemap([0-9]+)?\.xml$ /index.php?sitemap=$1&sitemap_n=$2 last;

    # Add trailing slash to */wp-admin requests.
    rewrite /wp-admin$ $scheme://$host$uri/ permanent;
    rewrite /files/(.*)$ http://content.jeremyfelt.com/uploads/$1 permanent;
    rewrite /uploads/(.*)$ http://content.jeremyfelt.com/uploads/$1 permanent;
    rewrite /wp-content/plugins/(.*)$ http://content.jeremyfelt.com/plugins/$1 permanent;

    # Directives to send expires headers and turn off 404 error logging.
    location ~* \.(js|css|png|jpg|jpeg|gif|ico)$ {
        expires 24h;
        log_not_found off;
    }

    # prevent .files and .directories from being served
    location ~ /\.          { access_log off; log_not_found off; deny all; }
    # prevent Vagrantfile from being served
    location = /Vagrantfile { access_log off; log_not_found off; deny all; }
    # prevent fabfile.py from being served
    location = /fabfile.py  { access_log off; log_not_found off; deny all; }
    # prevent provisioning from appearing
    location ~ /provision   { access_log off; log_not_found off; deny all; }
    # prevent configuration from appearing
    location ~ /config      { access_log off; log_not_found off; deny all; }

    # Pass uploaded files to wp-includes/ms-files.php.
    rewrite /files/$ /index.php last;

    if ($uri !~ wp-content/plugins) {
        rewrite /files/(.+)$ /wp-includes/ms-files.php?file=$1 last;
    }

    # Rewrite multisite '.../wp-.*' and '.../*.php'.
    if (!-e $request_filename) {
        rewrite ^/[_0-9a-zA-Z-]+(/wp-.*) $1 last;
        rewrite ^/[_0-9a-zA-Z-]+.*(/wp-admin/.*\.php)$ $1 last;
        rewrite ^/[_0-9a-zA-Z-]+(/.*\.php)$ $1 last;
    }

	location ~ \.php$ {
	    # Try the files specified in order. In our case, try the requested URI and if
	    # that fails, try (successfully) to pass a 404 error.
	    try_files      $uri =404;

	    # Include the fastcgi_params defaults provided by nginx
	    include        /etc/nginx/fastcgi_params;

	    # The amount of time for upstream to wait for a fastcgi process to send data.
	    # We keep this *extremely* high so that one can be lazy when remote debugging.
		fastcgi_read_timeout 3600s;
		# PRODUCTION
		# fastcgi_read_timeout 30s;

	    # Buffer size for reading the header of the backend FastCGI process.
	    # This defaults to the value of a single fastcgi_buffers, so does not
	    # need to be specified in our case, but it's good to be explicit.
		fastcgi_buffer_size 128k;

	    # The number and size of the buffers into which the reply from the FastCGI
	    # process in the backend is read.
	    #
	    # 4 buffers at 128k means that any reply by FastCGI greater than 512k goes
	    # to disk and replies under 512k are handled directly in memory.
	    fastcgi_buffers 4 128k;

	    # Use the upstream for php5-fpm that we defined in nginx.conf
	    fastcgi_pass   php;

	    # And get to serving the file!
	    fastcgi_index  index.php;
	}
}

# Handle any requests at the content domain
server {
    listen       80;
    server_name  content.jeremyfelt.com dev.content.jeremyfelt.com;
    root         /srv/web/jeremyfelt.com/content;

    access_log   /var/log/nginx/jeremyfelt-content-access.log;
    error_log    /var/log/nginx/jeremyfelt-content-error.log;

    location / {
        index index.php;
        try_files $uri $uri/ /index.php?$args;
    }

    location ~* \.(eot|ttf|woff)$ {
        add_header Access-Control-Allow-Origin http://jeremyfelt.com;
    }

    # Directives to send expires headers and turn off 404 error logging.
    location ~* \.(js|css|png|jpg|jpeg|gif|ico)$ {
        expires 24h;
        log_not_found off;
    }

    # this prevents hidden files (beginning with a period) from being served
    location ~ /\.          { access_log off; log_not_found off; deny all; }

	location ~ \.php$ {
	    # Try the files specified in order. In our case, try the requested URI and if
	    # that fails, try (successfully) to pass a 404 error.
	    try_files      $uri =404;

	    # Include the fastcgi_params defaults provided by nginx
	    include        /etc/nginx/fastcgi_params;

	    # The amount of time for upstream to wait for a fastcgi process to send data.
	    # We keep this *extremely* high so that one can be lazy when remote debugging.
		fastcgi_read_timeout 3600s;
		# PRODUCTION
		# fastcgi_read_timeout 30s;

	    # Buffer size for reading the header of the backend FastCGI process.
	    # This defaults to the value of a single fastcgi_buffers, so does not
	    # need to be specified in our case, but it's good to be explicit.
		fastcgi_buffer_size 128k;

	    # The number and size of the buffers into which the reply from the FastCGI
	    # process in the backend is read.
	    #
	    # 4 buffers at 128k means that any reply by FastCGI greater than 512k goes
	    # to disk and replies under 512k are handled directly in memory.
	    fastcgi_buffers 4 128k;

	    # Use the upstream for php5-fpm that we defined in nginx.conf
	    fastcgi_pass   php;

	    # And get to serving the file!
	    fastcgi_index  index.php;
	}
}

# Server up the tweets!
server {
    listen      80;
    server_name tweets.jeremyfelt.com dev.tweets.jeremyfelt.com;
    root        /srv/web/jeremyfelt.com/tweets;

    access_log  /var/log/nginx/tweets-access.log;
    error_log   /var/log/nginx/tweets-error.log;

    # Directives to send expires headers and turn off 404 error logging.
    location ~* \.(js|css|png|jpg|jpeg|gif|ico)$ {
        expires 24h;
        log_not_found off;
    }

    # this prevents hidden files (beginning with a period) from being served
    location ~ /\.          { access_log off; log_not_found off; deny all; }
}
